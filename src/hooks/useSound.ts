/**
 * useSound - Re-export from SoundContext
 * 
 * WHY: Provides a convenient import path for the sound hook.
 * This allows consumers to import from hooks/ instead of context/.
 */
export { useSound } from '../context/SoundContext';
